# Путь до файла со списком ID объявлений для обработки
ITEMS_FILE=data/items.txt
# Путь до файла с актуальными прокси
PROXIES_FILE=data/proxies.txt
# Файл, куда программа складывает заблокированные прокси
BLOCKED_PROXIES_FILE=data/blocked_proxies.txt

# Реквизиты подключения к PostgreSQL
DATABASE_DSN=postgresql://admin:Password123@81.30.105.134:5402/avito_zamer
# Схема, куда сохраняем объявления
DATABASE_SCHEMA=new
# Размер пула соединений asyncpg
DB_POOL_SIZE=30

# Максимальное число попыток обработки одного объявления
MAX_ATTEMPTS=5
# Таймаут ожидания загрузки страницы (секунды)
GOTO_TIMEOUT=15.0
# Количество одновременно работающих воркеров
WORKER_COUNT=4

# Режим запуска браузера Playwright (true=headless, false=с UI)
PLAYWRIGHT_HEADLESS=false
PLAYWRIGHT_DISPLAY_BASE=4

# Шаблон канонической ссылки на объявление Avito
BASE_ITEM_URL=https://www.avito.ru/{item_id}
